📝 Nota Técnica: Conexão MongoDB (VendaERP)
Problema: Erro SSL handshake failed / UNEXPECTED_EOF_WHILE_READING.

Causa: O servidor está configurado para conectar sem SSL/TLS, mas o cliente (Python/Compass) tentava iniciar uma negociação segura por padrão.

Diagnóstico: Ao desativar o SSL, o erro mudou de "Handshake" para "Authentication", provando que a rede estava alcançando o servidor, mas a segurança estava bloqueando o fluxo.

Solução: Forçar ssl=false e tls=false explicitamente na URI de conexão, seguindo o padrão exato exportado pelo MongoDB Compass.

Regra de Ouro: Se o servidor não espera SSL, qualquer tentativa de "Handshake" do cliente resulta em queda imediata da conexão (EOF).



Caminho que eu recomendo seguir
1. Escolher o “embrulho” desktop
Para Windows + algo simples de empacotar hoje:

Electron: mais pesado, mas documentação infinita e muito usado em apps tipo VSCode, Slack.
Tauri: muito mais leve, mas exige toolchain Rust.
Sugestão pra você agora: começar com Electron pela facilidade.
2. Primeiro MVP (sem mexer no Django)
Objetivo: ter um “Agro Mais PDV” instalável que só abre o sistema atual.
Passos:
Criar um projeto Electron mínimo:
Janela única, tela cheia, sem barra de URL.
Carrega a URL do Render ou do servidor local (http://127.0.0.1:8000/).
Configurar:
Ícone do app.
Bloqueio de abrir várias janelas (um app por caixa).
Atalhos básicos (F11, Ctrl+R desabilitados, etc.).
Gerar instalador .exe para Windows.
3. Começar a usar armazenamento local “forte”
Dentro desse app Electron:

Criar um pequeno módulo que salva em arquivo local ou SQLite:
Preferências importantes (ex.: layout de colunas, configurações avançadas de PDV).
Cache de alguns dados pesados (opcional).
A comunicação entre o site e o app pode ser via:
window.electronAPI expondo funções do lado Node (IPC).
4. Só depois pensar em recursos offline
Quando o shell estiver estável:

Desenhar filas locais para:
Gravar vendas/ajustes localmente quando o servidor cair.
Sincronizar assim que a conexão voltar.
