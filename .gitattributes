# Auto detect text files and perform LF normalization
* text=auto

# Docker / Linux: evita quebra de shebang e COPY em builds no Windows
Dockerfile text eol=lf
entrypoint.sh text eol=lf
.dockerignore text eol=lf
